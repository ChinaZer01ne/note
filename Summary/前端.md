## HTML
## CSS

### margin和padding
* margin
	* 元素外部或元素间的空间用margin
* padding
	* 元素内部，背景颜色等等用padding

### 内联元素和块元素

* 内联元素：元素只占本身内容需要的大小，挨个往后排。
	* 高度和宽度对内联元素没有影响；`margin`和`padding`只有水平方向的作用。
	* `a`、`img`、`strong`、`em`、`button`等等。
	* `display:inline`
* 块级元素：每个元素占一行，垂直堆砌。
	* `body`、`main`、`header`、`footer`、`section`、`nav`、`aside`、`div`、`h1`-`h6`、`p`、`ul`、`ol`、`li`等等。
	* `display:block`
* 内联块框（`inline-block boxes`）
	* 从外面看起来像内联，内部表现像块级元素；只占内容所需的空间，内部又具备块级元素的特点。
	* 这样的格式，我们就可以给内联元素设置margin和pading属性了。
	* img其实具备这种特性。
### 定位
* 默认定位
	* normal flow
	* `position:relative`
* 绝对定位
	* 不影响其他元素
	* out of flow
	* 默认是相对于viewport的定位。通过设置其第一个父元素的定位为relative，可以设置该元素绝对定位的相对容器。
	* `position:absolute`

### 布局
* `Float Layouts`
	* 虽然还在用，但是过时的
	* `float:left`、`float:right`
	* 浮动元素会out of flow，但是文本和内联元素会环绕在浮动元素周围（区别于绝对定位的不影响其他元素的特性）
	* 由于out of flow，容器不再调整元素的高度，高度会坍缩成0
* `Flex Box`
	* 适合一维布局
* `CSS Grid`
	* 适合二维布局
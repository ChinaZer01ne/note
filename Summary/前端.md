## HTML
## CSS

### margin和padding
* margin
	* 元素外部或元素间的空间用margin
* padding
	* 元素内部，背景颜色等等用padding

### 内联元素和块元素

* 内联元素：元素只占本身内容需要的大小，挨个往后排。
	* 高度he
* 块级元素：每个元素占一行，垂直堆砌。
	* `body`、`main`、`header`、`footer`、`section`、`nav`、`aside`、`div`、`h1`-`h6`、`p`、`ul`、`ol`、`li`等等。
	* `display:block`